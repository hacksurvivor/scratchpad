interface:
  display_name: "Visual Scratchpad"
  short_description: "Render, inspect, and visibly publish UI variants"
  default_prompt: "Use $visual-scratchpad to render and compare visual design variants, then read and show the exact final image once with show_image before drawing conclusions."

policy:
  allow_implicit_invocation: true
